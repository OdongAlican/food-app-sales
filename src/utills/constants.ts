export const accessTokenKey: string = "@access-token";
export const modalState: {
    purchase: string;
    edit: string;
    view: string;
} = {
    purchase: 'purchase',
    edit: 'edit',
    view: 'view',
}